apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mp-coupons-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: mp-coupons-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: mp_coupons-db-password
        version: latest
