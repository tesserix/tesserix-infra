apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mp-connector-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: mp-connector-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: mp_connector-db-password
        version: latest
