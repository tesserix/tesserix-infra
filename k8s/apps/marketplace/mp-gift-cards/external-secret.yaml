apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mp-gift-cards-secrets
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: mp-gift-cards-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: mp_gift_cards-db-password
        version: latest
