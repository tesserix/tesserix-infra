apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mp-tax-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: mp-tax-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: mp_tax-db-password
        version: latest
