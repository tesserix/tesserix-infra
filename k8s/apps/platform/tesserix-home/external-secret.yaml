apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tesserix-home-secrets
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: tesserix-home-secrets
    creationPolicy: Owner
  data:
    - secretKey: INTERNAL_SERVICE_KEY
      remoteRef:
        key: shared-internal-service-key
        version: latest
